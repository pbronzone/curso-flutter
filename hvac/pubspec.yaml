name: hvac
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  floor: ^1.4.2
  flutter:
    sdk: flutter
  gap: ^3.0.1
  go_router: ^14.1.4
dev_dependencies:
  build_runner: ^2.1.2
  floor_generator: ^1.4.2
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
