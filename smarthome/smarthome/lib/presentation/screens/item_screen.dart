import 'package:flutter/material.dart';

class ItemScreen extends StatelessWidget {
  const ItemScreen({super.key});
  static const String name = 'item_screen';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
